# === CONFIGURATION DJANGO ===
DEBUG=False
DJANGO_SECRET_KEY=votre_clef_ultra_secrete_generee

# === HÔTES AUTORISÉS ===
# Mets ici ton domaine Render/Railway + localhost pour tests
ALLOWED_HOSTS=localhost,127.0.0.1,gestock.onrender.com

# === BASE DE DONNÉES ===
# Cette variable sera automatiquement définie par Render/Railway,
# donc inutile de la modifier manuellement après déploiement.
# Exemple local (si tu veux tester avant) :
# DATABASE_URL=postgresql://postgres:motdepasse@localhost:5432/gestock_db

# === PARAMÈTRES EMAIL (optionnel, pour notifications futures) ===
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=ton_email@gmail.com
# EMAIL_HOST_PASSWORD=motdepasse
# EMAIL_USE_TLS=True
